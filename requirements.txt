# DocuMind - Intelligent OCR Platform
# Requirements for Phase 1 (DeepSeek-OCR with PyTorch)

# ============================================================
# Web Framework
# ============================================================
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
jinja2>=3.1.0

# ============================================================
# Data Validation & Configuration
# ============================================================
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ============================================================
# PyTorch & ML
# ============================================================
# Note: Install PyTorch separately for your CUDA version:
# https://pytorch.org/get-started/locally/
# Example for CUDA 11.8:
# pip install torch --index-url https://download.pytorch.org/whl/cu118
torch>=2.6.0
torchvision>=0.19.0

# ============================================================
# Transformers & Model Dependencies
# ============================================================
# Pinned versions per DeepSeek-OCR requirements
# transformers==4.46.3
# tokenizers==0.20.3
# safetensors>=0.4.0

# Qwen3-VL Dependencies (Optional - Install separately)
# Qwen3-VL engine is the default. To use it, install dependencies from:
#   pip install -r requirements-qwen.txt
#
# See requirements-qwen.txt for detailed installation instructions.
# IMPORTANT: Transformers version conflict
# - Qwen3-VL requires transformers>=4.56.0 (see requirements-qwen.txt)
# - PaddleOCR-VL requires transformers==4.55.0 (see requirements-paddleocr.txt)
# - If you need both engines, use separate virtual environments or switch versions

# DeepSeek-OCR dependencies
einops>=0.7.0
addict>=2.4.0
easydict>=1.10

# ============================================================
# Image Processing
# ============================================================
pillow>=10.0.0
opencv-python>=4.8.0
scikit-image>=0.21.0

# ============================================================
# Optional: Flash Attention (CUDA only)
# ============================================================
# Install separately if using CUDA:
# pip install flash-attn==2.7.3 --no-build-isolation
# flash-attn==2.7.3

# ============================================================
# Engine-Specific Dependencies (Optional - Install separately)
# ============================================================
# Qwen3-VL engine (default):
#   pip install -r requirements-qwen.txt
#   See requirements-qwen.txt for detailed installation instructions.
#
# PaddleOCR-VL engine (optional):
#   pip install -r requirements-paddleocr.txt
#   See requirements-paddleocr.txt for detailed installation instructions.
#
# Note: The app requires at least one engine to be installed.
# If no engines are installed, the app will fail to start.

# ============================================================
# Development Dependencies (optional)
# ============================================================
# Uncomment for development:
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
httpx>=0.27.0  # Required for Ollama client
# black>=24.0.0
# ruff>=0.3.0
# mypy>=1.9.0

