# DocuMind - Intelligent OCR Platform
# Requirements for Phase 1 (DeepSeek-OCR with PyTorch)

# ============================================================
# Web Framework
# ============================================================
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
jinja2>=3.1.0

# ============================================================
# Data Validation & Configuration
# ============================================================
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ============================================================
# PyTorch & ML
# ============================================================
# Note: Install PyTorch separately for your CUDA version:
# https://pytorch.org/get-started/locally/
# Example for CUDA 11.8:
# pip install torch --index-url https://download.pytorch.org/whl/cu118
torch>=2.6.0
torchvision>=0.19.0

# ============================================================
# Transformers & Model Dependencies
# ============================================================
# Pinned versions per DeepSeek-OCR requirements
# transformers==4.46.3
# tokenizers==0.20.3
# safetensors>=0.4.0

# Qwen3-VL dependencies
# Note: Qwen3-VL requires latest transformers from source
# Install with: pip install git+https://github.com/huggingface/transformers
accelerate>=0.30.0
qwen-vl-utils[decord]==0.0.8
# For non-Linux systems, use: qwen-vl-utils (without decord)
bitsandbytes>=0.41.0

# DeepSeek-OCR dependencies
einops>=0.7.0
addict>=2.4.0
easydict>=1.10

# ============================================================
# Image Processing
# ============================================================
pillow>=10.0.0
opencv-python>=4.8.0
scikit-image>=0.21.0

# ============================================================
# Optional: Flash Attention (CUDA only)
# ============================================================
# Install separately if using CUDA:
# pip install flash-attn==2.7.3 --no-build-isolation
# flash-attn==2.7.3

# ============================================================
# PaddleOCR-VL Dependencies (Optional - Install separately)
# ============================================================
# PaddleOCR-VL engine is optional. To use it, install dependencies from:
#   pip install -r requirements-paddleocr.txt
#
# See requirements-paddleocr.txt for detailed installation instructions.
# The app will work without PaddleOCR-VL (using Qwen3-VL as default).

# ============================================================
# Development Dependencies (optional)
# ============================================================
# Uncomment for development:
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
httpx>=0.27.0  # Required for Ollama client
# black>=24.0.0
# ruff>=0.3.0
# mypy>=1.9.0

